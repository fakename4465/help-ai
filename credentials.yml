# This file contains credentials for the messaging channels and NLU services.
# For local development and testing with the shell, you often don't need to modify this.

rest:
  # This entry is needed if you want to use the REST channel.
  # For example, to connect a custom UI or use `curl`.
  # rasa:
  #   url: "http://localhost:5005/webhooks/rest/webhook"

action_endpoint:
  url: "http://localhost:5055/webhook" # Tells Rasa where your action server is running